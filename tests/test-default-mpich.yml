name: Test default MPI setup (mpich)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: setup-mpi
        uses: .
        # No mpi input — should default to mpich on Linux

      - name: Assert output mpi equals mpich
        env:
          ACTUAL: ${{ steps.setup-mpi.outputs.mpi }}
        run: |
          echo "actual: $ACTUAL"
          if [ "$ACTUAL" != "mpich" ]; then
            echo "FAIL: expected 'mpich', got '$ACTUAL'"
            exit 1
          fi
          echo "PASS"

      - name: Assert mpicc is available
        run: |
          if ! command -v mpicc > /dev/null 2>&1; then
            echo "FAIL: mpicc not found in PATH"
            exit 1
          fi
          echo "PASS: mpicc found at $(command -v mpicc)"

      - name: Assert mpichversion runs
        run: |
          mpichversion
          echo "PASS: mpichversion succeeded"
